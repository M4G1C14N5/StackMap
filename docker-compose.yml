services:
  stackmap:
    build: .
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - stackmap-net

networks:
  stackmap-net:
    driver: bridge
